{% extends 'base.html.twig' %}

{% block title %}Credit{% endblock %}

{% block body %}
    <h1>Credit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ credit.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ credit.Quantite }}</td>
            </tr>
            <tr>
                <th>DateUpdate</th>
                <td>{{ credit.DateUpdate ? credit.DateUpdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_credit_index') }}">back to list</a>

    <a href="{{ path('app_credit_edit', {'id': credit.id}) }}">edit</a>

    {{ include('credit/_delete_form.html.twig') }}
{% endblock %}
