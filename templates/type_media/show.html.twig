{% extends 'base-backend.html.twig' %}

{% set title = "TypeMedia " %}
{% block title %}TypeMedia{% endblock %}

{% block content %}
    <h1>TypeMedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_media.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ type_media.Code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_media.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_media_index') }}">back to list</a>

    <a href="{{ path('app_type_media_edit', {'id': type_media.id}) }}">edit</a>

    {{ include('type_media/_delete_form.html.twig') }}
{% endblock %}
