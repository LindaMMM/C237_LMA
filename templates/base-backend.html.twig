<!DOCTYPE html>
<html>
   <head>
      <meta charset="UTF-8">  
        <title>{% block title %}Back - Mes DVD!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
   </head>
   <body>
   <div class="bg-white">
      <heaher>
        <nav>
        <a href="{{ path('home') }}" aria-label="Page d'accueil">
          <img src={{asset('images/logo.svg')}} alt="logo"/>
          <span aria-hidden="true" class="text-lg lg:text-xl">
            DVD <strong>BACK<span class="text-blue-600">.</span></strong>
          </span>
        </a>
        <ul id="toggled-menu" class="w-full absolute top-full left-0 -translate-y-full -z-10 text-gray-800 border-b border-gray-200 flex flex-col items-center md:static md:z-10 md:w-min md:transform-none md:border-none md:flex-row">
        <li class="py-4 md:py-0 md:mr-6">
          <a href="{{ path('movies') }}" class="text-sm uppercase font-semibold w-full hover:text-rose-600">Films</a>
        </li>
        <li class="py-4 md:py-0 md:mr-6">
          <a href="{{ path('sales') }}" class="text-sm uppercase font-semibold w-full hover:text-rose-600">Commande</a>
        </li>
        <li class="py-4 md:py-0 md:mr-6">
          <a href="{{ path('types') }}" class="text-sm uppercase font-semibold w-full hover:text-rose-600">Types</a>
        </li>
        <li class="py-4 md:py-0 md:mr-6">
          <a 
          href="{{ path('users') }}"
          class="text-sm uppercase font-semibold w-full hover:text-rose-600">Utilisateurs
          </a>
        </li>
      </ul>
        </nav>
      </header>
      <main>
        <acticle>
        <header>
          <h2>{{title}}</h2>
        </header>
            {% block content %}{% endblock %}
      
        </article>
        <aside>
        </aside>
      </main>
      <footer>
        <nav>
        </nav>
      
      
      
      
         <p>&nbsp;<br>Copyright DVDs {{ "now"|date("Y") }}&nbsp;<br></p>
      </footer>
  </div>
   </body>
</html>
