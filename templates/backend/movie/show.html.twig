{% extends 'base-backend.html.twig' %}

{% set title = 'Film' %}

{% block content %}
    
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ movie.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ movie.summary }}</td>
            </tr>
            <tr>
                <th>DateSortie</th>
                <td>{{ movie.dateSortie ? movie.dateSortie|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ movie.enable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
<div class="inline-flex rounded-md shadow-sm" role="group">
    <a class="inline-flex m-2 p-5 items-center btn btn-blue" href="{{ path('app_movie_index') }}">retour à la liste</a>

    <a class="inline-flex m-2 p-5 items-center btn btn-blue"  href="{{ path('app_movie_edit', {'id': movie.id}) }}">modifier</a>
</div>
{% endblock %}
