{% extends 'base-backend.html.twig' %}


{% set title = "client " %}

{% block content %}
    <h1>Edit Client</h1>

    {{ include('backend/client/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('backend/client/_delete_form.html.twig') }}
    <div class="inline-flex rounded-md shadow-sm" role="group">
        <a class="inline-flex m-2 p-5 items-center btn btn-blue" href="{{ path('app_client_index') }}">back to list</a>
    </div>
{% endblock %}
