<form method="post" action="{{ path('app_type_credit_delete', {'id': type_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_credit.id) }}">
    <button class="btn">Delete</button>
</form>
